# 上游 API 地址（默认 https://api.anthropic.com）
AGENTEVAL_UPSTREAM=https://api.anthropic.com

# 代理监听端口（默认 57633）
AGENTEVAL_PORT=57633

# 日志存放目录（默认 ~/.agenteval/logs）
# AGENTEVAL_LOG_DIR=/path/to/logs

# 详细模式：1 或 true 开启（打印 request body）
# AGENTEVAL_VERBOSE=1
